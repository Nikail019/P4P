# Codex Project Instructions

Use `CLAUDE.md` as the canonical project context and keep it updated when session-relevant facts change.

Current priority: semantic mapping correctness for PMI/GD&T, not display polish. Do not treat a visual highlight as proof of correctness; use `<save-prefix>_diagnostics.json` and tests as the source of truth.
